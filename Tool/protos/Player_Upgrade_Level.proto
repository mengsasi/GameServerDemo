syntax = "proto3";

package gameProto;

import "Item.proto";

message Player_Upgrade_Level {
	//request
	
	//response
	// r=1,物品不足 r=2,等级已达上限
	int32 r = 1;
	int32 level = 2;
	repeated gameProto.Item items = 3;
}